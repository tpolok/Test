dist: trusty
sudo: false

language: node_js
node_js:
  - '8'

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable


image: node:latest


before_script:
     - cd untitled1
     - npm install --silent
mabuild:
         stage: build
     script:
         - cd untitled1
         - npm run build --prod
